specVersion: "v1"
serviceId: "sidequests"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sidequests-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
